package com.information.basic;

// This is not functional interface so here we cannot use lambda, but we can use anonymous inner class
public interface Employee {

    String getName();
    int getSalary();
}
